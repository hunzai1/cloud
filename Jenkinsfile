pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'Building cloud_project...'
                // Add build commands here, e.g., compiling your project
            }
        }
        stage('Test') {
            steps {
                echo 'Testing cloud_project...'
                // Add test commands here, e.g., running test scripts
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying cloud_project...'
                // Add deployment commands here, e.g., sending files to the server
            }
        }
    }
}
